{{- if .Values.identityApi.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "dawning.fullname" . }}-identity-api
  labels:
    {{- include "dawning.identityApi.labels" . | nindent 4 }}
spec:
  {{- if not .Values.identityApi.autoscaling.enabled }}
  replicas: {{ .Values.identityApi.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "dawning.identityApi.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
        checksum/secret: {{ include (print $.Template.BasePath "/secrets.yaml") . | sha256sum }}
      labels:
        {{- include "dawning.identityApi.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: identity-api
          image: "{{ .Values.global.imageRegistry }}{{ .Values.identityApi.image.repository }}:{{ .Values.identityApi.image.tag }}"
          imagePullPolicy: {{ .Values.identityApi.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.identityApi.service.port }}
              protocol: TCP
          env:
            {{- range $key, $value := .Values.identityApi.env }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
            # 从 ConfigMap 注入
            - name: ConnectionStrings__MySQL
              value: "Server=$(DATABASE_HOST);Port=$(DATABASE_PORT);Database=$(DATABASE_NAME);User=$(DATABASE_USERNAME);Password=$(DATABASE_PASSWORD);SslMode=Preferred;"
            - name: Redis__Configuration
              value: "$(REDIS_HOST):$(REDIS_PORT)"
            - name: Kafka__BootstrapServers
              value: "$(KAFKA_BOOTSTRAP_SERVERS)"
          envFrom:
            - configMapRef:
                name: {{ include "dawning.fullname" . }}-config
            - secretRef:
                name: {{ include "dawning.fullname" . }}-secrets
          {{- with .Values.identityApi.livenessProbe }}
          livenessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.identityApi.readinessProbe }}
          readinessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          resources:
            {{- toYaml .Values.identityApi.resources | nindent 12 }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
