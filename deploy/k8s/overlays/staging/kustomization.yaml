apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: dawning

resources:
  - ../../base

namePrefix: staging-

commonLabels:
  environment: staging

# Moderate replicas for staging
replicas:
  - name: identity-api
    count: 2
  - name: gateway-api
    count: 2
  - name: admin-frontend
    count: 2

patches:
  - target:
      kind: ConfigMap
      name: dawning-config
    patch: |-
      - op: replace
        path: /data/ASPNETCORE_ENVIRONMENT
        value: "Staging"
  # Fix init container service names with namePrefix
  - target:
      kind: Deployment
      name: identity-api
    patch: |-
      - op: replace
        path: /spec/template/spec/initContainers/0/command
        value: ['sh', '-c', 'until nc -z staging-mysql 3306; do echo waiting for mysql; sleep 2; done']
      - op: replace
        path: /spec/template/spec/initContainers/1/command
        value: ['sh', '-c', 'until nc -z staging-redis 6379; do echo waiting for redis; sleep 2; done']
      - op: replace
        path: /spec/template/spec/initContainers/2/command
        value: ['sh', '-c', 'until nc -z staging-kafka 9092; do echo waiting for kafka; sleep 2; done']
  - target:
      kind: Deployment
      name: gateway-api
    patch: |-
      - op: replace
        path: /spec/template/spec/initContainers/0/command
        value: ['sh', '-c', 'until nc -z staging-mysql 3306; do echo waiting for mysql; sleep 2; done']
      - op: replace
        path: /spec/template/spec/initContainers/1/command
        value: ['sh', '-c', 'until nc -z staging-redis 6379; do echo waiting for redis; sleep 2; done']
      - op: replace
        path: /spec/template/spec/initContainers/2/command
        value: ['sh', '-c', 'until nc -z staging-kafka 9092; do echo waiting for kafka; sleep 2; done']
